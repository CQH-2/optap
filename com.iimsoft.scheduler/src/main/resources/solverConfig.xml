<?xml version="1.0" encoding="UTF-8"?>
<solver>
    <solutionClass>com.iimsoft.scheduler.domain.ProductionSchedule</solutionClass>
    <entityClass>com.iimsoft.scheduler.domain.HourPlan</entityClass>

    <scoreDirectorFactory>
        <scoreDrl>rules/productionConstraints.drl</scoreDrl>
    </scoreDirectorFactory>

    <constructionHeuristic>
        <constructionHeuristicType>FIRST_FIT</constructionHeuristicType>
    </constructionHeuristic>

    <localSearch>
        <unionMoveSelector>
            <cartesianProductMoveSelector>
                <changeMoveSelector variableName="item"/>
                <changeMoveSelector variableName="quantity"/>
            </cartesianProductMoveSelector>
            <changeMoveSelector variableName="item"/>
            <changeMoveSelector variableName="quantity"/>
        </unionMoveSelector>
        <acceptor>
            <lateAcceptanceSize>500</lateAcceptanceSize>
        </acceptor>
        <forager>
            <acceptedCountLimit>1000</acceptedCountLimit>
        </forager>
    </localSearch>

    <termination>
        <secondsSpentLimit>10</secondsSpentLimit>
    </termination>
</solver>